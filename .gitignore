__pycache__/
*.pyc
.ipynb_checkpoints/

# MLflow tracking store (mlflow.db, mlruns/) + data snapshots -- all under
# data/, regenerated by scripts/refresh_and_retrain.py. Leading slash so
# this only matches the repo-root data/ dir, not qlstm_stock/data/ etc.
/data/
